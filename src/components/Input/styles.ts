import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    width: '100%',
    fontFamily: 'ReemKufyBold',
    padding: 5,
    backgroundColor: '#fff',
    fontSize: 16
  }
});